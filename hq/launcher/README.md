# dreamteamer machine

The workspaces on a hosted dreamteamer machine: each is a folder under `/workspaces`, opened in its own
browser tab. Create one from the dreamteamer template, clone a repository, open, switch, delete.
Built into the `hq` image; not published.
