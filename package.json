{
	"name": "@dreamteamer/images",
	"version": "0.4.1",
	"private": true,
	"description": "The template images `dt start container <name> --template <t>` runs — public, built by CI on a v* tag, pushed to ghcr.io/dreamteamer.",
	"license": "Apache-2.0",
	"type": "module",
	"scripts": {
		"test": "node --test",
		"test:image": "HQ_IMAGE=${HQ_IMAGE:-hq:sec} node --test tests/container.test.mjs && tests/smoke.sh ${HQ_IMAGE:-hq:sec}"
	},
	"engines": { "node": ">=20" }
}
