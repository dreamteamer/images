{
	"name": "@dreamteamer/images",
	"version": "0.2.0",
	"private": true,
	"description": "The template images `dt start container <name> --template <t>` runs — public, built by CI on a v* tag, pushed to ghcr.io/dreamteamer.",
	"license": "Apache-2.0",
	"type": "module",
	"scripts": {
		"test": "node --test"
	},
	"engines": { "node": ">=20" }
}
